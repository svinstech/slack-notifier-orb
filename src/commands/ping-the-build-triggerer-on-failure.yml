description: >
  

steps:
  - run:
      name: Get data about the build triggerer
      environment:
        WORKFLOW_ID: CIRCLE_WORKFLOW_ID
        TOKEN: SLACK_NOTIFIER_TOKEN
      command: <<include(scripts/ping_the_build_triggerer.sh)>>
